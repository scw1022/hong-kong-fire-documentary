# News Scraper Environment Configuration
# Copy this file to .env and fill in your values:
#   cp env.example .env
#
# NEVER commit .env to git (it's in .gitignore)

# Required: Your GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens?type=beta
# Permissions needed: Contents (read/write), Pull requests (read/write)
GITHUB_TOKEN=your_github_token_here

# Required: Your fork's repository path
# Format: username/repo-name
FORK_REPO=your-username/Hong-Kong-Fire-Documentary

# Optional: Upstream repository (has default)
# UPSTREAM_REPO=Hong-Kong-Emergency-Coordination-Hub/Hong-Kong-Fire-Documentary

# Optional: Branch name for pull requests (has default)
# PR_BRANCH=scraper-updates

# Optional: Main branch name (has default)
# MAIN_BRANCH=main

